const prefix = 'chat_system';
export const LOCAL_STORAGE_USER_INFO = `${prefix}-user-info`;

export const LOCAL_STORAGE_USER_TOKEN = `${prefix}-user-token`;

export  const LOCAL_STORAGE_LOGIN_AVATAR = `${prefix}-login-avatar`;

export  const LOCAL_STORAGE_CHAT_LIST = `${prefix}-chat-list`;